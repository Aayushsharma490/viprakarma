"use client";

// DISABLED: Google Translate was causing DOM manipulation conflicts with React
// Our custom LanguageContext provides all needed translation functionality
export default function GoogleTranslate() {
  // Component disabled to prevent conflicts with React's DOM updates
  return null;
}
